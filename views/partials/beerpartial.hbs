<section>
    <div class="beer-partial">
        <h1>{{this.name}}</h1>
        <img class="beer_image" src={{this.image_url}}>
        <div class="beer_tag">{{this.tagline}}</div>
        <p>{{this.description}}</p>
    </div>
</section>
<p>Hola</p>